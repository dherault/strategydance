# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    # The head check stops forks, but `pull_request` runs the workflow from the pull request's
    # own code, so anybody who can push a branch can edit this file and print the service
    # account. Scoping the secret to an environment is what fixes that, and only once the
    # environment carries a protection rule: add required reviewers to `preview` in the
    # repository settings, or this line buys nothing
    environment: preview
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          # Pins CI to the packageManager version in package.json, as check-pull-request does
          bun-version-file: package.json
      # `bun install` generates the Data Connect SDK through postinstall, which the build needs
      - run: bun install --frozen-lockfile && bun run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_STRATEGYDANCE }}
          projectId: strategydance
